---
description: Revê as alterações por commitar contra as regras do CLAUDE.md antes de dares como terminado
---

Corre `git status` e `git diff` para veres as alterações por commitar.

Revê-as contra as regras do CLAUDE.md deste repositório, em particular:
1. Se tocam em UI: confirma que foram testadas visualmente com a extensão do Chrome ligada ao Claude Code. Se não foram, diz isso claramente e NÃO dês a tarefa como concluída só porque compila.
2. Se tocam em regras de produto/negócio (entrada pelo professor, ativação = projeto com 3+ secções, pricing): confirma que não contradizem o que já está definido.
3. Qualidade geral do código e consistência com convenções do projeto.

Termina com um veredicto curto: pronto para commit / falta X.
